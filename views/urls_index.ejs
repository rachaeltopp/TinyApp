<html lang="en">
<head>
    <title>TinyApp</title>
</head>
<body>
    <header>
        <% include partials/_header %>
    </header>
    <!-- Add EJS code here -->
    <% for (key in urls) { %>
        <p><%= key %> --> <%= urls[key] %> 
            <form method="POST" action="/urls/<%= key %>/delete">
                <button>delete</button>
                <a href="/urls/<%= key %>">edit</a>
            </form> 
        </p>
    <% } %>

    <a href="/urls/new"> Get a new shortened URL here! </a>
</body>
</html>